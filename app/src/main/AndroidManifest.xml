<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icono_inicio"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icono_inicio"
        android:supportsRtl="true"
        android:theme="@style/Theme.DbEjercicio">

        <activity
            android:name=".UsuarioActivity"
            android:label="Reservar Producto"
            android:theme="@style/Theme.DbEjercicio" />
        <activity
            android:name=".HistorialActivity"
            android:label="Historial"
            android:theme="@style/Theme.DbEjercicio" />
        <activity
            android:name=".PagarActivity"
            android:label="Pagar"
            android:theme="@style/Theme.DbEjercicio" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.DbEjercicio" />

<activity
    android:name=".LoginActivity"
    android:label="Reserva"
    android:theme="@style/Theme.DbEjercicio"
    android:exported="true">
    <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
    </intent-filter>
</activity>
    </application>
</manifest>